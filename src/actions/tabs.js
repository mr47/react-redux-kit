/**
 * Created by mr470 on 02.04.2016.
 */

import { createAction, handleActions, handleAction } from 'redux-actions';
import { SETUP_TABS } from '../reducers';

export const setupTabs = createAction(SETUP_TABS);


